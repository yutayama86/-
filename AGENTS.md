# イバトコ実装・公開ルール

このリポジトリで作業する自動化ツールとAIエージェントは、`CLAUDE.md` の確定仕様とあわせて次のルールを守る。

## ニュース公開の安全基準

- 架空の出来事、発言、数値、制度、店舗、人物を生成しない。
- 日付、数値、制度、運行、料金、試合・イベント情報は一次情報で確認する。
- 他媒体の記事を丸写し、または表現だけを変えて再掲載しない。事実を確認し、イバトコ独自の解説を加える。
- 公式写真、ロゴ、キャラクター、SNS画像は、利用条件を確認せずに使用しない。
- `sourceUrls` に確認した情報源URLと表示名を保持する。
- `draft`、`reviewed`、`noindex` の公開制御を削除・迂回しない。
- 実装確認用サンプルは `sample: true` かつ `noindex: true` とし、実際のニュースと誤認させない。
- 既存URL、canonical、構造化データ、TOPの確定デザインを破壊しない。
- 公開前に `npm run check` を実行し、型エラーとビルドエラーを解消する。
- `npm run audit:site` で、内部リンク、見出し、メタ情報、JSON-LD、画像altを可能な範囲で確認する。

## ニュース追加時の基本手順

1. `src/content/news/` にMarkdownまたはMDXを追加する。
2. 作成中は `draft: true` とする。
3. 一次情報と本文を照合し、`sourceUrls` を記録する。
4. 編集責任者が確認した後に `reviewed: true` とする。
5. 公開時だけ `draft: false` とする。
6. `npm run check` と `npm run audit:site` を通す。
