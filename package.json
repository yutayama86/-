{
  "name": "ibatoco",
  "type": "module",
  "version": "0.1.0",
  "description": "イバトコ - 現地から、信頼を編む。茨城の地域価値編集ブランド",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "check": "astro check && astro build",
    "audit:site": "npm run build && node scripts/quality-audit.mjs",
    "deploy": "npm run audit:site && npx wrangler deploy",
    "preview": "astro preview",
    "astro": "astro",
    "db:seed": "node scripts/csv-to-d1-seed.mjs",
    "og:build": "node scripts/build-og-images.mjs",
    "og:check": "node scripts/check-ogp.mjs"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "astro": "^7.1.6",
    "astro-og-canvas": "^0.13.0",
    "sharp": "0.35.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "topojson-client": "^3.1.0",
    "topojson-server": "^3.0.1",
    "topojson-simplify": "^3.0.3",
    "typescript": "^5.9.3"
  }
}
