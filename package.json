{
  "name": "shikumi-base",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "シクミベース - 中小企業・地域企業の集客・営業・業務・発信を再現可能な仕組みに変える事業ブランド",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "check:functions": "tsc --noEmit --strict --lib ES2022,DOM --module ESNext --moduleResolution bundler --target ES2022 functions/api/contact.ts",
    "og": "bash tools/build-og.sh",
    "quality": "node scripts/quality-check.mjs",
    "audit:seo": "node scripts/seo-audit.mjs",
    "growth": "node scripts/daily-growth.mjs",
    "topic": "node scripts/select-daily-topic.mjs",
    "verify": "npm run test && npm run check && npm run check:functions && npm run build && npm run quality && npm run audit:seo",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=shikumi-base --branch=main",
    "astro": "astro",
    "test": "node --test scripts/tests/*.test.mjs",
    "improve": "node scripts/improve-page.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^7.1.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.9.3",
    "wrangler": "^4.125.0"
  }
}
